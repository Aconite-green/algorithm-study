# get a positive number(10~99)

while True:
    no = int(input('value ? :'))
    if no >= 10 and no <=99:
        break

print(f'vlaue is {no}')

# if 10 <= no <= 99:
# if not(no < 10 or no > 99):

# structured programming : Three types of control flow are used: sequential, selection, and iteration.